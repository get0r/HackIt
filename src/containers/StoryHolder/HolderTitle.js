import React from 'react';

const HolderTitle = ({ title }) => {
    return (
        <div className="text-gray-700 p-4 font-mono font-semibold text-xl">
            { title }
        </div>
    );
};

export default HolderTitle;
